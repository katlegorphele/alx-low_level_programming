#include "main.h"
/**
  * _strchr - locates a character in a string
  * @s: source string
  * @c: target chararcter
  * Return: @c else NULL if character not found
  */

char *_strchr(char *s, char c)
{
	unsigned int i;

	for (i = 0; *(s + 1) != '\0'; i++)
		if (*(src + 1) == c)
			return (s + 1);
}
